/**
 * Represents a base class for value objects.
 * 
 * In Domain-Driven Design (DDD), a `ValueObject` is an object that is identified
 * by its values rather than a unique identity. This abstract class serves as a
 * foundation for implementing specific value objects.
 */
export default abstract class ValueObject { }
